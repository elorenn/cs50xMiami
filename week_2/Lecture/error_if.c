// mon 1/30/17 week 2

#include <stdio.h>
#include <cs50.h>

/**
 * This program contains an error which prevents it from executing properly.
 * 
 * Accepts user input in the form of an integer and outputs a message if
 * the input is larger than 5.
 * Author: Ken Loomis
 */ 
int main ( )
{
	int num = GetInt( );
	if ( num > 5 )
	;
	{
		printf ( "Bigger than 5.\n" );
	}
}
